import React from 'react';
import styles from './styles/Settings.module.css';

export const Settings = props => {
	return (
		<div className={styles.container}>
			<h2 className={styles.temp}>In Development...</h2>
		</div>
	);
};
